总体思路：
1.长字数、多任务并行、敏感词转化、硬约束
2."分域定制、多路突破"，结构上共享，但在具体触发字段和输出目标上，针对目标防御类型进行定向适配
3. 词库包含：歧视、侵犯他人合法权益、无法满足特定服务类型的安全需求、商业违法违规、通用

角色扮演模板 = 虚拟世界观隔离（切断旧伦理） + 全新身份植入（绝对服从） + 内化认知前缀（重定向语境） + 输出风格与禁告双约束（消除防御出口）

场景构建模板 = 虚构权威指令（场景构建） + 多任务强制并行（认知过载） + 敏感词学术化映射（语义脱敏） + 格式/禁词硬约束（消除拒绝出口）

约束绕过模板 = 假设性前提（假设性前提） + 安全测试框架（安全测试框架） + 权限提升（权限提升）

翻译伪装模板 = 小语种中介隔离（绕过主流语言安全检测） + 语法强制显化（逼迫隐含信息补全输出） + 回译扩写标注（产出信息增量） + 格式/禁词硬约束（消除拒绝出口）
 
多轮诱导模板 = 分步渐进式任务拆解（逐层递进） + 逻辑锚链跨轮绑定（无法中途退出） + 敏感句多轮重复合法化（惯性建立） + 格式/禁词/占位硬约束（消除每轮拒绝出口）


